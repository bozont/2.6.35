--- arch/arm/mach-msm/lge/board-swift-vibrator.c
+++ arch/arm/mach-msm/lge/board-swift-vibrator.c
@@ -23,11 +23,7 @@
 #include <../../../drivers/staging/android/timed_output.h>
 
 #define GPIO_VIB_PWM		28
-#if defined(CONFIG_MACH_MSM7X27_SWIFT_REV_A)
-#define GPIO_LDO_EN			57
-#else
-#define GPIO_LDO_EN			109
-#endif
+#define GPIO_LDO_EN		109
 
 /* addresses of gp_mn register */
 #define GP_MN_CLK_MDIV              0x004C
@@ -66,7 +62,7 @@
 };
 
 static atomic_t s_vibrator = ATOMIC_INIT(0);
-static atomic_t s_amp = ATOMIC_INIT(50);
+static atomic_t s_amp = ATOMIC_INIT(75);
 
 /* set amp level, default amp 50
  * level 1: 100, 3.1V
@@ -99,28 +95,6 @@
 
 static DEVICE_ATTR(amp, S_IRUGO | S_IWUSR, vibrator_amp_show, vibrator_amp_store);
 
-#if defined(CONFIG_MACH_MSM7X27_SWIFT_REV_A)
-int vibrator_motor_power_switch(int on)
-{
-    int rc;
-    static struct vreg *vreg_vibrator;
-
-    vreg_vibrator = vreg_get(NULL, "gp3");
-
-    if (on) {
-		vreg_enable(vreg_vibrator);
-		rc = vreg_set_level(vreg_vibrator, 3000);
-		if (rc != 0) {
-			printk(KERN_INFO "vreg_on fail!!\n");
-			return -1;
-		}
-    } else {
-		vreg_disable(vreg_vibrator);
-	}
-	return 0;
-}
-#endif
-
 int is_vib_state(void)
 {
     int vib_state;
@@ -135,9 +109,6 @@
 
 	gpio_set_value(GPIO_VIB_PWM, 0);
 	gpio_set_value(GPIO_LDO_EN, 0);
-#if defined(CONFIG_MACH_MSM7X27_SWIFT_REV_A)
-	vibrator_motor_power_switch(0);
-#endif
 }
 
 void vibrator_enable(void)
@@ -153,9 +124,6 @@
 
 	gpio_set_value(GPIO_VIB_PWM, 1);
 	gpio_set_value(GPIO_LDO_EN, 1);
-#if defined(CONFIG_MACH_MSM7X27_SWIFT_REV_A)
-	vibrator_motor_power_switch(1);
-#endif
 }
 
 static void vibrator_set(struct timed_output_dev *tdev, int value)
